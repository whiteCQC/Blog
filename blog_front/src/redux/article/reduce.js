const todos = (state = [], action) => {
    switch (action.type) {
        case '':
            return {
                ...state,
            }
        case '':
        default:
            return state
    }
}

export default todos